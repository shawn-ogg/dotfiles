# Lean Neovim Config

Optimized for:
- Bash
- Python
- Markdown
- YAML / GitHub Actions

## Install

```bash
mv ~/.config/nvim ~/.config/nvim-old
cp -r nvim-lean ~/.config/nvim
```

## Dependencies

```bash
brew install neovim ripgrep
```
